# Personal Notes API

A production-ready Personal Notes API built with Node.js, Express, and Prisma.

## Setup
1. Clone the repository
2. Run \`npm install\`
3. Copy \`.env.example\` to \`.env\` and update the variables
4. Run \`npm run dev\` to start the development server
